#include <stdio.h>

int main(){

int a,b;

    printf("Enter two Numbers :");
    scanf("%d %d",&a,&b);

    if(a>b) printf("%d is greater and %d is smaller",a,b);
    else if(b>a) printf("%d is greater than %d",b,a);
      else if (a = b) printf(" the given number are equal");

      

return 0;
    
} 